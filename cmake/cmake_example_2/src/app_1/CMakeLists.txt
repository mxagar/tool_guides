SET( SRC_DIR ".." )

SET(EXEC_NAME ${APP1_NAME})

SET(files
    program1.cpp
)

SOURCE_GROUP("Source Files" FILES ${files})

INCLUDE_DIRECTORIES( 
    ${SRC_DIR}
    ${LIB1_INCDIR}
    ${LIB2_INCDIR}
)

LINK_DIRECTORIES(
    ${LIB1_LIBDIR}
    ${LIB2_LIBDIR}
)

ADD_EXECUTABLE(${EXEC_NAME}
    ${files}
)

TARGET_LINK_LIBRARIES(${EXEC_NAME}
    ${LIB1_NAME}
    ${LIB2_NAME}
)

INSTALL(TARGETS ${EXEC_NAME} RUNTIME DESTINATION ${PROJECT_BINDIR})
